;(function () {
    'use strict';

    angular
        .module('app')
        .controller('LegendCtrl', LegendCtrl);

    LegendCtrl.$inject = ['$scope'];

    function LegendCtrl($scope) {
        let vm = this;
    }
})();
